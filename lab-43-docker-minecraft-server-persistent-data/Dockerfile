# This Dockerfile is not strictly needed for this lab.
# But it demonstrates how you could extend the base image.
FROM itzg/minecraft-server:latest

# Add any custom configurations or scripts here
# For example, you could copy a custom server.properties file
# COPY server.properties /data/server.properties

# Or install additional software
# RUN apt-get update && apt-get install -y some-package

# The base image handles the Minecraft server startup
